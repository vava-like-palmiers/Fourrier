
    img = imread('db\apple-1.gif');
    [xb,yb] = baricentre(img_db{im});
    xb
    yb
    pos = [xb yb];
    img_db{im} = insertMarker(img_db{im}, pos);
    [S, X, Y] = signature(img_db{im}, xb, yb);
    M=[X Y]
    
    clf;imshow(img_db{im});
    disp(label_db{im}); 
    drawnow();


